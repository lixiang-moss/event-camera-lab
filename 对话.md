我要连接两台DAVIS346,那是不是要写两个ros节点？我现在安装在docker中的这个驱动是怎么运行的？我是可以直接查看相机的画面吗？这个驱动是以什么形式存在的？






12:14






耗时 30秒



对，两台 DAVIS346 不是要重新写两个 C++ ROS 节点，而是要在 launch 里启动两个 driver 节点实例。
你现在的驱动运行逻辑是这样：
Docker Ubuntu 20.04
  -> ROS Noetic
  -> catkin build 编译 rpg_dvs_ros
  -> roslaunch 启动 davis_ros_driver
  -> driver 通过 libcaer 访问 USB 相机
  -> 发布 ROS topics
当前默认脚本：
./scripts/launch_live_stream.sh
实际会在容器里执行类似：
roslaunch event_camera_lab_bringup current_live_stream.launch
而这个 launch 现在只启动一个节点：
<node name="event_camera_driver"
      pkg="davis_ros_driver"
      type="davis_ros_driver"
      output="screen">
  <param name="serial_number" value="" />
</node>
这里的含义是：
pkg="davis_ros_driver"：ROS 包名
type="davis_ros_driver"：编译出来的可执行程序
serial_number=""：不指定序列号，驱动会尝试连接任意一台 DAVIS
namespace 默认是 /dvs
所以会发布类似：/dvs/events
/dvs/image_raw
/dvs/camera_info
/dvs/imu

两台相机时，应该改成两个节点实例，例如：
<node ns="camera_0" name="event_camera_driver" pkg="davis_ros_driver" type="davis_ros_driver">
  <param name="serial_number" value="第一台序列号" />
</node>

<node ns="camera_1" name="event_camera_driver" pkg="davis_ros_driver" type="davis_ros_driver">
  <param name="serial_number" value="第二台序列号" />
</node>
这样 topic 会变成：
/camera_0/events
/camera_0/image_raw
/camera_0/camera_info
/camera_0/imu

/camera_1/events
/camera_1/image_raw
/camera_1/camera_info
/camera_1/imu
你当然可以查看“画面”，但要注意 DAVIS 不是普通 RGB 相机：
/image_raw 是 DAVIS 的普通灰度帧，也就是 APS frame，可以用 rqt_image_view 看。
/events 是事件流，本身不是普通图片，需要用 dvs_renderer 渲染成可视化图像。
所以“看画面”有两种：看普通灰度图：rqt_image_view /dvs/image_raw
看事件图像：启动 dvs_renderer，再看 renderer 输出的 image topic

驱动本身的存在形式是：它不是 Docker 服务里的独立后台程序，而是 rpg_dvs_ros 里的一个 ROS package：
ros_ws/src/rpg_dvs_ros/davis_ros_driver/
编译后生成 ROS 可执行节点：
davis_ros_driver
同时也有 nodelet 形式，但我们当前默认用的是普通 ROS node 形式。Docker 只是提供 Ubuntu 20.04 + ROS Noetic + libcaer 的运行环境，真正访问相机的是容器里启动的 davis_ros_driver 进程。


12:14








所以应该怎么办？有时需要一台，有时需要两台






12:33






建议做成两种启动 profile，不要每次改代码：
一台相机 -> single profile
两台相机 -> dual profile
核心思路是：驱动只需要一份，节点实例可以启动一个或两个。
一台时：
/dvs/events
/dvs/image_raw
/dvs/camera_info
两台时：
/camera_0/events
/camera_0/image_raw
/camera_0/camera_info

/camera_1/events
/camera_1/image_raw
/camera_1/camera_info
我建议下一步这样改项目：
保留当前默认单相机启动方式
继续让：
./scripts/launch_live_stream.sh
默认启动一台相机，方便日常快速测试。
新增一个双相机 launch 文件
例如：
ros_ws/src/event_camera_lab_bringup/launch/current_dual_live_stream.launch
里面启动两个 davis_ros_driver 节点：
<node ns="camera_0" name="event_camera_driver" pkg="davis_ros_driver" type="davis_ros_driver">
  <param name="serial_number" value="$(arg camera_0_serial)" />
  <param name="master" value="True" />
</node>

<node ns="camera_1" name="event_camera_driver" pkg="davis_ros_driver" type="davis_ros_driver">
  <param name="serial_number" value="$(arg camera_1_serial)" />
  <param name="master" value="False" />
  <remap from="reset_timestamps" to="/camera_0/reset_timestamps" />
</node>
在启动脚本里增加 profile
以后你就可以这样用：
CAMERA_PROFILE=current_single ./scripts/launch_live_stream.sh
或：
CAMERA_PROFILE=current_dual EXTRA_ARGS="camera_0_serial:=00000889 camera_1_serial:=00000xxx" ./scripts/launch_live_stream.sh
先找出两台相机各自的 serial number
最简单方法是：一次只插一台相机，启动 driver，看日志里打印的设备 ID / serial。两台都记录下来后，再双相机启动。
你现在这个项目应该发展成这样：
同一套 Docker
同一份 davis_ros_driver
同一个 launch 脚本入口

根据 CAMERA_PROFILE 选择：
  - 单相机
  - 双相机
  - 以后其他型号相机
所以答案是：不用写两个新驱动，也不用维护两套工程。只需要增加一个双相机 launch/profile，并用 serial number 精确绑定两台设备。


12:33








启动脚本是哪个？profile又是什么？serial number是固定的是吗？即使我更换usb插口的接线顺序也能识别到？ 






12:37






启动脚本是这个：
/home/lx/ec_xiangli/scripts/launch_live_stream.sh
你平时在项目根目录运行：
cd /home/lx/ec_xiangli
./scripts/launch_live_stream.sh
这就是启动相机 driver 的入口。
profile 不是 ROS 官方概念，是我们在脚本里自己定义的“启动模式”。比如：
CAMERA_PROFILE=current_davis ./scripts/launch_live_stream.sh
意思是：使用当前单相机 DAVIS 测试配置。
现在项目里大概是这个逻辑：
CAMERA_PROFILE=current_davis
  -> 启动 event_camera_lab_bringup/current_live_stream.launch
  -> 启动一台 davis_ros_driver
  -> 发布 /dvs/events 等 topic
以后可以增加：
CAMERA_PROFILE=current_dual
  -> 启动 current_dual_live_stream.launch
  -> 启动两台 davis_ros_driver
  -> 发布 /camera_0/events 和 /camera_1/events
所以 profile 可以理解成：“这次我要用哪种相机启动方案”。
关于 serial number：是的，它是相机硬件自己的固定序列号，通常不会因为你换 USB 插口、换接线顺序而改变。
这点很重要：
不指定 serial number：
  driver 可能连接到系统发现的第一台相机
  两台相机时顺序可能不稳定

指定 serial number：
  camera_0 永远绑定某一台物理相机
  camera_1 永远绑定另一台物理相机
  即使 USB 口顺序换了，角色也不会乱
所以两台相机时应该这样设计：
camera_0_serial = 第一台 DAVIS346 的固定序列号
camera_1_serial = 第二台 DAVIS346 的固定序列号
然后启动时明确写：
CAMERA_PROFILE=current_dual \
EXTRA_ARGS="camera_0_serial:=00000889 camera_1_serial:=00000xxx" \
./scripts/launch_live_stream.sh
你现在要先做的一步是：分别单独插入每台相机，启动 driver，看日志里打印的 DAVIS serial number，并记录下来。
例如你之前日志里出现过类似：
DAVIS-00000889
那么这台相机的 serial 很可能就是：
00000889
一句话总结：启动脚本是 scripts/launch_live_stream.sh；profile 是我们定义的启动模式；serial number 是相机固定身份；两台相机必须用 serial number 绑定，USB 接口顺序才不会影响左右/编号关系。


12:37








请你把/home/lx/ec_xiangli/ros_ws/src/rpg_dvs_ros下所有的README.md文件，都在其对应的文件夹中建立一份READMECN.md文件。该文件是起对应的中文翻译。






12:42






耗时 1分钟 33秒



已完成，在每个 README.md 所在目录都新增了对应中文翻译：
[ros_ws/src/rpg_dvs_ros/READMECN.md](/home/lx/ec_xiangli/ros_ws/src/rpg_dvs_ros/READMECN.md)
[ros_ws/src/rpg_dvs_ros/dvs_calibration/READMECN.md](/home/lx/ec_xiangli/ros_ws/src/rpg_dvs_ros/dvs_calibration/READMECN.md)
[ros_ws/src/rpg_dvs_ros/dvs_calibration_gui/READMECN.md](/home/lx/ec_xiangli/ros_ws/src/rpg_dvs_ros/dvs_calibration_gui/READMECN.md)
我只新增了这 3 个文件，没有改动原来的 README.md。当前它们还未提交到 git。

已编辑 3 个文件+201-0查看更改撤销审核

ros_ws/src/rpg_dvs_ros/READMECN.mdros_ws/src/rpg_dvs_ros/READMECN.md+124-0
ros_ws/src/rpg_dvs_ros/dvs_calibration/READMECN.mdros_ws/src/rpg_dvs_ros/dvs_calibration/READMECN.md+72-0
ros_ws/src/rpg_dvs_ros/dvs_calibration_gui/READMECN.mdros_ws/src/rpg_dvs_ros/dvs_calibration_gui/READMECN.md+5-0





12:43











使用时说缺少标定文件。是必须标定才能看到画面吗？我并没有看到画面






13:38






耗时 36秒



不，缺少标定文件不是必须问题，不标定也应该能看到图像/事件流。
你看到的“缺少标定文件”一般是类似：
/root/.ros/camera_info/DAVIS-xxxx.yaml not found
它的意思只是：这台相机还没有内参标定文件，所以 /dvs/camera_info 里没有准确的相机内参。它会影响：
去畸变
双目标定/双目几何
需要准确相机模型的算法
但它不应该阻止：
/dvs/events
/dvs/image_raw
事件流显示
普通灰度帧显示
你截图里更像是 rqt_image_view 没有选到正确 topic，或者当前没有 image topic 在发布。中间那个窗口左上角下拉框是空的，你需要点它，选择：
/dvs/image_raw
如果你启动了 renderer，则选择：
/dvs_rendering
或者类似名字的 rendering topic。
建议你按这个顺序检查：
cd /home/lx/ec_xiangli
./scripts/check_topics.sh
重点看有没有这些 topic：
/dvs/events
/dvs/image_raw
/dvs/camera_info
再看频率：
./scripts/open_shell.sh
rostopic hz /dvs/events
rostopic hz /dvs/image_raw
判断方式：
/dvs/events 有频率
  -> 相机事件流是通的

/dvs/image_raw 有频率
  -> 普通灰度画面也在发布

/dvs/events 有频率，但 /dvs/image_raw 没频率
  -> 事件流正常，但 APS frame 没开或没有正常出帧

两个都没频率
  -> driver 没真正连上相机或相机没有发布数据
在 rqt_reconfigure 里，你可以点左边的 davis_ros_driver，然后确认这几个参数：
aps_enabled = True
dvs_enabled = True
exposure = 5000 左右
frame_interval = 25000 左右
如果你想看事件图，而不是普通灰度图，最好用：
CAMERA_PROFILE=current_davis_with_renderer ./scripts/launch_live_stream.sh
然后在 rqt_image_view 里看：
/dvs_rendering
一句话：不需要先标定才能看到画面。现在先确认 topic 是否在发布，并在 rqt_image_view 下拉框里选 /dvs/image_raw 或 /dvs_rendering。


13:39
